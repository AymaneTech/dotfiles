session_name: visa
windows:
  - window_name: default
    panes:
      - shell_command:
          - cd ~/codes/work/visa-direct-integration
          - eval "$(ssh-agent -s)"
          - ssh-add ~/.ssh/gitlab_progressoft

